/**
 * @file   rsa.c
 * @Author Auzias (mael@auzias.net)
 * @date   May, 2014
 * @brief  Main code file of a simple RSA implementation in C
 *
 * This file contains main function and functions needed for the implementation
 * of a RSA implementation in C.
 */

#include "rsa.h"

int main(int argc, char *argv ) {
    printf("Hello World\n");
    return 0;
}

/**
 * 
 * 
 */
int key_generation(key *k) {
  
  uint32_t p = 0;
  uint32_t q = 0;
  return p;
}